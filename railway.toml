[build]
builder = "NIXPACKS"

[build.nixPacks]
packages = ["python311"]  # Solo Python, sin extras

[deploy]
startCommand = "gunicorn -w 2 -b 0.0.0.0:$PORT webhook_server:app --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10